import _ from 'lodash';


function data(text) {
	for (let i = 0, i < text.length, i += 1){
		if (text.length[i] === text.length[i].toUpperCase){
			text.length[i].toLowerCase;
			}
		return;
	}	
}


const text = 'oneWAsfAASf';

//data(text);

console.log(data(text));
